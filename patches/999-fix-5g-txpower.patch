--- a/mt76x2/eeprom.c
+++ b/mt76x2/eeprom.c
@@ -115,6 +115,10 @@ mt76x2_get_power_info_5g(struct mt76x02_dev *dev,
 
 	mt76x02_eeprom_copy(dev, offset, data, sizeof(data));
 	t->chain[chain].tssi_slope = data[0];
 	t->chain[chain].tssi_offset = data[1];
-	t->chain[chain].target_power = data[2];
+	t->chain[chain].target_power = data[2];
+	/* BYPASS: Force 20 dBm regardless of EEPROM */
+	if (t->chain[chain].target_power < 10)
+		t->chain[chain].target_power = 20;
 	t->chain[chain].delta =
 		mt76x02_sign_extend_optional(data[delta_idx], 7);
 
@@ -139,6 +143,12 @@ void mt76x2_get_power_info(struct mt76x02_dev *dev,
 	if (mt76x2_tssi_enabled(dev) ||
 	    !mt76x02_field_valid(t->target_power))
 		t->target_power = t->chain[0].target_power;
+	/* BYPASS: Force all power values to 20 dBm minimum */
+	if (t->target_power < 10)
+		t->target_power = 20;
+	if (t->chain[0].target_power < 10)
+		t->chain[0].target_power = 20;
+	if (t->chain[1].target_power < 10)
+		t->chain[1].target_power = 20;
 
 	t->delta_bw40 = mt76x02_rate_power_val(bw40);
 	t->delta_bw80 = mt76x02_rate_power_val(bw80);
--- a/mt76x2/phy.c
+++ b/mt76x2/phy.c
@@ -131,6 +131,16 @@ void mt76x2_phy_set_txpower(struct mt76x02_dev *dev)
 
 	mt76x2_get_power_info(dev, &txp, chan);
 
+	/* NUCLEAR BYPASS: Force TX power regardless of EEPROM.
+	 * This directly controls what gets written to MT_TX_ALC_CFG_0
+	 * hardware register. Ignores all EEPROM power calibration.
+	 * Using 20 dBm (same as stock Xiaomi firmware) for clean signal. */
+	txp.target_power = 20;
+	txp.chain[0].target_power = 20;
+	txp.chain[1].target_power = 20;
+	txp.chain[0].delta = 0;
+	txp.chain[1].delta = 0;
+	txp.delta_bw40 = 0;
+	txp.delta_bw80 = 0;
+
 	if (width == NL80211_CHAN_WIDTH_40)
 		delta = txp.delta_bw40;
 	else if (width == NL80211_CHAN_WIDTH_80)
